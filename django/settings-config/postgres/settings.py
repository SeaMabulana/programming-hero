DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': 'postgres', # db name of choice
        'USER': 'postgres',
        'PASSWORD': 'password@123', # same pasword as created in the postgres db
        'HOST': 'localhost',
        'PORT': '5432',
    }
}